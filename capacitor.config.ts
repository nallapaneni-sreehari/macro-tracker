import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.iamsreehari.macrotracker',
  appName: 'MacroTracker',
  webDir: 'www'
};

export default config;
